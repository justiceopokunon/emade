# --- Site metadata ---
NEXT_PUBLIC_SITE_NAME=E-MADE
NEXT_PUBLIC_SITE_DESCRIPTION=Interactive e-waste learning lab
NEXT_PUBLIC_SITE_URL=https://emade.social

# --- Admin authentication ---
# Prefer a private ADMIN_PASS; the NEXT_PUBLIC fallback only exists for local demos.
ADMIN_PASS=
NEXT_PUBLIC_ADMIN_PASS=slingshot-admin

# --- External services ---
# Neon connection string (postgresql://user:password@host/db?sslmode=require&channel_binding=require)
DATABASE_URL=
POSTGRES_URL=
# Vercel Blob read/write token for uploads & PDF generation
BLOB_READ_WRITE_TOKEN=
# Gemini API key for story, hero, and DIY generation
GEMINI_API_KEY=
